# Project Headers Cheatsheet

- Use `"..."` for headers you create.
- Put declarations in `.h` files.
- Put definitions in `.c` files.
- Add include guards to avoid double inclusion.

Example guard:

```c
#ifndef MYHEADER_H
# define MYHEADER_H

#endif
```
