# System Headers Cheatsheet

- Use `<...>` for system headers.
- Common low-level header: `<unistd.h>`.
- `write()` is declared in `<unistd.h>`.
- System headers are installed with the compiler or OS.

Include pattern:

```c
#include <unistd.h>
```
