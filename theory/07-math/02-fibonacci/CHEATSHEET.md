# Fibonacci Cheatsheet

Definition:
- `F(0) = 0`
- `F(1) = 1`
- `F(n) = F(n-1) + F(n-2)`

Iterative version:
- keep `a` and `b`
- compute `next = a + b`
- shift `a = b`, `b = next`

Use cases:
- sequence generation
- recursion practice
- dynamic programming examples
