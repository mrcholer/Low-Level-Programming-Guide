# Square Root Cheatsheet

Use `sqrt()` for floating-point results.
Use integer methods for low-level integer-only code.

Integer idea:
- find largest `x` with `x * x <= n`
- binary search is efficient

Example:
- `4 -> 2`
- `5 -> 2`
- `15 -> 3`
